# Alpha Sentinel OS 구현 체크리스트 (Implementation Checklist)

이 문서는 `Alpha Sentinel OS`의 남은 구현 작업들을 단계별로 정리한 로드맵입니다.

## 🛠️ Phase 1: 환경 설정 및 인프라 (Setup & Infrastructure)

- [ ] **환경 변수 설정 (`.env`)**
    - [ ] `DATABASE_URL`: Neon DB 연결 문자열 설정
    - [ ] `OPENAI_API_KEY`: GPT-4 사용을 위한 키 설정
    - [ ] `FRED_API_KEY`: FRED 경제 데이터 키 설정
    - [ ] `DART_API_KEY`: Open DART 공시 데이터 키 설정
    - [ ] `API_SECRET_KEY`: 내부 API 통신용 보안 키 생성 및 설정

- [ ] **데이터베이스 초기화**
    - [ ] Neon Console 또는 로컬 터미널에서 `scripts/init_db.sql` 실행하여 테이블 생성

- [ ] **라이브러리 설치**
    - [ ] Backend: `pip install -r requirements.txt` (터미널에서 실행)
    - [ ] Frontend: `npm install` (터미널에서 실행, 의존성 충돌 해결 후)

---

## 🐍 Phase 2: 백엔드 로직 구현 (Backend)

### 2.1. 데이터 수집기 (Collectors) - `/api/collectors/`
- [ ] **`market.py` (Market Data)**
    - [ ] `yfinance`를 사용하여 S&P 500 (`^GSPC`), NASDAQ (`^IXIC`) 등의 현재가 및 과거 데이터 조회 함수 구현.
    - [ ] 개별 종목(Ticker)의 OHLCV 데이터 수집 함수 구현.
- [ ] **`fred.py` (Macro Data)**
    - [ ] `requests`로 FRED API 호출.
    - [ ] 주요 지표(미국채 10년/2년 금리, CPI, 실업률) 수집 함수 구현.
- [ ] **`dart.py` (Disclosure)**
    - [ ] Open DART API를 통해 특정 기업의 정기 공시 목록 조회 함수 구현.

### 2.2. 계산 엔진 (Calculators) - `/api/calculators/`
- [ ] **`finance.py`**
    - [ ] Pandas를 활용하여 재무제표 데이터 기반 PER, PBR, ROE, GPA 계산 로직 구현.
- [ ] **`macro.py`**
    - [ ] FRED 데이터의 전년 대비 등락률(YoY), 장단기 금리차 계산 및 정규화(Normalization) 로직 구현.

### 2.3. AI 에이전트 (Agents) - `/api/agents/`
- [ ] **`macro.py` (Macro Sentry)**
    - [ ] 수집된 Macro 데이터를 프롬프트에 넣어 시장 상태(Risk-On/Off)를 판단하는 LangChain 체인 구현.
- [ ] **`strategist.py` (Sector Strategist)**
    - [ ] 시장 상태에 따라 유망 섹터를 추천하는 로직 구현.
- [ ] **`insight.py` (Insight Analyst)**
    - [ ] 뉴스/공시 텍스트를 요약하고 긍/부정 감성을 분석하는 로직 구현.
- [ ] **`cio.py` (CIO)**
    - [ ] 하위 에이전트들의 의견을 종합(Ensemble)하여 최종 매수/매도/보류 가설을 생성하는 로직 구현.

### 2.4. API 연동 - `/api/index.py`
- [ ] Flask/FastAPI 라우트 생성
    - [ ] `POST /api/analyze`: 전체 분석 파이프라인 트리거.
    - [ ] `GET /api/market`: 프론트엔드용 단순 시세 조회.

---

## ⚛️ Phase 3: 프론트엔드 개발 (Frontend)

### 3.1. 서비스 레이어 - `/app/services/`
- [ ] **`api.ts`**
    - [ ] Backend API (`/api/...`)를 호출하고 에러를 처리하는 `fetch` 래퍼 함수 구현.

### 3.2. UI 컴포넌트 - `/app/components/`
- [ ] **`market-card.tsx`**
    - [ ] 실제 데이터를 받아 상승/하락 색상 및 화살표 아이콘을 동적으로 표시하도록 수정.
- [ ] **`agent-logs.tsx`**
    - [ ] Backend에서 스트리밍되거나 DB에 저장된 에이전트 사고 과정 로그를 실시간으로 보여주는 UI 구현.

### 3.3. 페이지 구현
- [ ] **대시보드 (`/app/(dashboard)/page.tsx`)**
    - [ ] `useEffect` 또는 Server Component를 사용하여 실제 `fetchMarketData` 호출 및 렌더링.
- [ ] **투자 가설 (`/app/thesis/page.tsx`)**
    - [ ] DB에 저장된 과거 투자 가설 목록을 카드 형태로 나열하는 UI 구현.

---

## 🚀 Phase 4: 통합 및 테스트
- [ ] **전체 파이프라인 테스트**
    - [ ] `api_test.py` 등을 활용하여 DB 연결 -> 데이터 수집 -> AI 판단 -> DB 저장 흐름 검증.
- [ ] **Vercel 배포 확인**
    - [ ] `vercel dev`로 로컬에서 Serverless Function 동작 확인.
